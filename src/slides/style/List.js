import { List as BaseList, ListItem as BaseItem } from 'spectacle';
import styled from 'styled-components';

export const List = styled(BaseList)``;

export const ListItem = styled(BaseItem)`
  margin-bottom: 20px;
`;
